# دليل مناقشة مشروع NOVA التعليمي

## PHP وMySQL والجلسات والصلاحيات والنشر

**إعداد:** المهندس طاهر القاضي  
**المشروع:** NOVA Training Institute  
**التقنية:** PHP تقليدي، MySQL، PDO، HTML، CSS، JavaScript بسيط  
**الرابط التشغيلي:** `https://novaeslz.duckdns.org/`

> هذا الدليل مكتوب للمراجعة قبل المناقشة. يشرح فكرة المشروع، بنيته، طريقة عمل الصفحات، قاعدة البيانات، المصادقة، الصلاحيات، الأمان، التصميم، النشر، والأسئلة التي قد يطرحها المناقش.

---

## 1. الملخص الذي تبدأ به المناقشة

مشروع NOVA هو منصة تعليمية عربية لمعهد تدريبي. تسمح المنصة للزائر بتصفح الموقع والدورات، وتسمح للمستخدم بإنشاء حساب وتسجيل الدخول وتعديل ملفه الشخصي والتسجيل في الدورات ومتابعة دوراته. كما تحتوي على لوحة إدارة محمية تسمح للمشرف بإدارة المستخدمين والدورات والتصنيفات والمدربين والتسجيلات والرسائل.

بُني المشروع باستخدام PHP التقليدي لأن الهدف هو فهم دورة الطلب كاملة من المتصفح إلى الخادم ثم إلى قاعدة البيانات، من دون إخفاء التفاصيل خلف إطار عمل. استُخدمت MySQL لتخزين البيانات، واستُخدمت PDO مع `prepared statements` لتقليل مخاطر حقن SQL. أما كلمات المرور فتُخزن بعد تجزئتها باستخدام `password_hash`، ويتم التحقق منها باستخدام `password_verify`.

الفكرة الأساسية هي أن النظام لا يعرض المحتوى نفسه لكل المستخدمين. فالزائر يرى المحتوى العام، والمستخدم المسجل يرى حسابه ودوراته، والمشرف يرى وظائف الإدارة. هذا يحقق مبدأ **التحكم في الوصول حسب الدور**.

---

## 2. أهداف المشروع

يحقق المشروع الأهداف التالية:

1. تقديم واجهة عامة للزوار لعرض المعهد والدورات والتواصل.
2. إنشاء حسابات مستخدمين مع التحقق من البيانات.
3. تسجيل الدخول والخروج باستخدام جلسات PHP.
4. تخزين كلمات المرور بطريقة آمنة نسبياً باستخدام خوارزمية التجزئة المدمجة في PHP.
5. إنشاء ملف شخصي يستطيع المستخدم تعديل بياناته الأساسية فيه.
6. تمكين المستخدم من التسجيل في الدورات ومتابعة حالة التسجيل.
7. توفير لوحة إدارة للمشرف.
8. تطبيق عمليات الإضافة والتعديل والحذف والعرض على بيانات الموقع.
9. منع الوصول إلى الصفحات المحمية من غير المستخدمين أو غير المشرفين.
10. نشر التطبيق على خادم يعمل عليه Apache وPHP وMySQL مع HTTPS.

---

## 3. المستخدمون والصلاحيات

يحتوي النظام عملياً على مستويين رئيسيين من المستخدمين، إضافة إلى الزائر غير المسجل.

| الفئة | ما يمكنها فعله |
|---|---|
| الزائر | فتح الصفحة الرئيسية، مشاهدة الدورات، مشاهدة صفحة عن المعهد، إرسال رسالة، فتح التسجيل أو الدخول |
| المستخدم | تسجيل الدخول، تعديل الملف الشخصي، مشاهدة لوحة الحساب، التسجيل في دورة، مشاهدة دوراته، تسجيل الخروج |
| المشرف `admin` | كل وظائف المستخدم، إضافة وتعديل وحذف الدورات والتصنيفات والمدربين، إدارة المستخدمين وتفعيلهم، إدارة التسجيلات والرسائل |

يُخزن الدور في العمود `role` داخل جدول `users`. القيمة الافتراضية هي `user`، ولا يصبح الحساب مشرفاً إلا إذا كانت قيمة الدور `admin`.

التحقق من الدور لا يعتمد على إخفاء الرابط فقط. فحتى لو كتب المستخدم رابط لوحة الإدارة يدوياً، تقوم الصفحة باستدعاء `requireAdmin()` وتعيده إلى صفحة الدخول إذا لم يكن مشرفاً.

---

## 4. بنية المشروع والملفات المهمة

البنية الأساسية للمشروع هي:

```text
nova/
├── index.php                 الصفحة الرئيسية
├── courses.php               قائمة الدورات مع البحث والفلترة
├── course.php                تفاصيل دورة واحدة
├── register.php              إنشاء حساب
├── login.php                 تسجيل الدخول
├── logout.php                تسجيل الخروج
├── profile.php               تعديل الملف الشخصي
├── dashboard.php             لوحة المستخدم
├── my-courses.php            الدورات المسجل فيها المستخدم
├── enroll.php                تنفيذ التسجيل في دورة
├── about.php                 عن المعهد
├── contact.php               نموذج التواصل
├── includes/
│   ├── config.php            الإعدادات والجلسة
│   ├── db.php                اتصال PDO بقاعدة البيانات
│   ├── auth.php              دوال الدخول والصلاحيات
│   ├── functions.php         دوال مساعدة عامة
│   ├── header.php             الرأس والتنقل
│   └── footer.php             التذييل
├── admin/
│   ├── index.php             لوحة الإدارة الرئيسية
│   ├── users.php             إدارة المستخدمين
│   ├── courses.php           إدارة الدورات
│   ├── categories.php        إدارة التصنيفات
│   ├── instructors.php       إدارة المدربين
│   ├── enrollments.php       إدارة التسجيلات
│   └── messages.php          إدارة الرسائل
├── database/
│   └── schema.sql            مخطط قاعدة البيانات
├── assets/css/
│   └── style.css             التنسيق العام
├── uploads/                  الملفات المرفوعة
├── .htaccess                 حماية وإعدادات Apache
└── README.md                 تعليمات الإعداد
```

الفائدة من تقسيم `includes` هي منع تكرار الكود. فبدلاً من كتابة الاتصال بقاعدة البيانات داخل كل صفحة، تستدعي الصفحة `db.php`. وبدلاً من تكرار رأس وتذييل الموقع، تستدعي `header.php` و`footer.php`.

---

## 5. دورة تنفيذ صفحة PHP

عند فتح المستخدم مثلاً صفحة `courses.php` تحدث الخطوات التالية:

1. يرسل المتصفح طلب HTTP إلى Apache.
2. يمرر Apache الملف إلى PHP-FPM.
3. تنشئ الصفحة ثابت `NOVA_APP` حتى تسمح للملفات الداخلية بالعمل.
4. تستدعي الصفحة `config.php` لقراءة الإعدادات وبدء الجلسة.
5. تستدعي `db.php` لإنشاء اتصال PDO.
6. تقرأ الصفحة قيم البحث والتصنيف من `GET`.
7. تبني استعلاماً SQL بمعاملات قابلة للربط.
8. تنفذ الاستعلام في MySQL.
9. تمرر النتائج إلى قالب HTML.
10. يرسل PHP صفحة HTML إلى المتصفح، ويحمّل المتصفح ملف CSS والصور.

هذا يوضح أن PHP تعمل على الخادم، بينما HTML وCSS الناتجان يصلان إلى المتصفح. المستخدم لا يرى كود PHP الأصلي؛ يرى الناتج النهائي فقط.

---

## 6. ملف الإعداد `includes/config.php`

هذا الملف مسؤول عن الإعدادات المركزية. من أهم ما يحتويه:

```php
define('DB_HOST', getenv('NOVA_DB_HOST') ?: 'localhost');
define('DB_USER', getenv('NOVA_DB_USER') ?: 'root');
define('DB_PASS', getenv('NOVA_DB_PASS') ?: '');
define('DB_NAME', getenv('NOVA_DB_NAME') ?: 'nova_db');
```

يُفضّل استخدام متغيرات البيئة في الإنتاج حتى لا تُكتب كلمات المرور داخل مستودع GitHub. إذا لم يجد التطبيق متغير البيئة فإنه يستخدم قيمة احتياطية محلية، وهذا مناسب للتطوير فقط وليس للنشر النهائي.

ويحتوي الملف على عنوان الموقع:

```php
define('SITE_URL', getenv('NOVA_SITE_URL') ?: 'https://novaeslz.duckdns.org');
```

تُستخدم قيمة `SITE_URL` لبناء الروابط الداخلية والصور وملفات CSS. لذلك إذا كانت القيمة خاطئة فقد تعمل الصفحات بدون تنسيق أو تقود الروابط إلى استضافة قديمة.

كما يضبط الملف خصائص الجلسة:

```php
session_set_cookie_params([
    'lifetime' => 0,
    'path' => '/',
    'secure' => $isHttps,
    'httponly' => true,
    'samesite' => 'Lax',
]);
```

معنى ذلك أن ملف تعريف الجلسة يُرسل عبر HTTPS عند استخدام HTTPS، ولا تستطيع JavaScript العادية قراءته بسبب `HttpOnly`، وتُخفف `SameSite=Lax` بعض مخاطر إرسال الجلسة من مواقع خارجية.

في الإنتاج يتم تعطيل عرض الأخطاء للمستخدم وتسجيلها بدلاً من إظهار تفاصيل قاعدة البيانات على الشاشة:

```php
ini_set('display_errors', 0);
ini_set('log_errors', 1);
```

---

## 7. الاتصال بقاعدة البيانات باستخدام PDO

في `includes/db.php` ينشئ التطبيق كائن PDO:

```php
$pdo = new PDO(
    "mysql:host=" . DB_HOST . ";dbname=" . DB_NAME . ";charset=utf8mb4",
    DB_USER,
    DB_PASS,
    [
        PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION,
        PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
        PDO::ATTR_EMULATE_PREPARES => false,
    ]
);
```

اختيار PDO مفيد للأسباب التالية:

- يدعم MySQL وقواعد بيانات أخرى.
- يوفر معاملات مربوطة تمنع إدخال قيمة المستخدم مباشرة داخل SQL.
- يسمح بالتعامل مع الأخطاء عبر الاستثناءات.
- يعيد الصفوف بصيغة associative array سهلة القراءة.
- تعطيل `ATTR_EMULATE_PREPARES` يجعل التحضير الحقيقي للبيانات مفضلاً.

مثال آمن من التسجيل:

```php
$stmt = $pdo->prepare(
    "SELECT id FROM users WHERE email = ?"
);
$stmt->execute([$email]);
```

القيمة الموجودة في `$email` لا تُلصق داخل نص الاستعلام، بل تُرسل كمعامل منفصل. لذلك إذا كتب المستخدم نصاً يشبه SQL فلن يتحول إلى جزء من الاستعلام.

---

## 8. قاعدة البيانات والعلاقات

### جدول المستخدمين `users`

يخزن الاسم والبريد وكلمة المرور المجزأة والدور والصورة وبيانات الاتصال وتاريخ الميلاد وحالة التفعيل.

| العمود | الوظيفة |
|---|---|
| `id` | رقم المستخدم، مفتاح أساسي متزايد |
| `full_name` | الاسم الكامل |
| `email` | البريد، وفريد لمنع التكرار |
| `password` | كلمة المرور المجزأة |
| `role` | `user` أو `admin` |
| `phone` | رقم الهاتف |
| `city` | المدينة |
| `gender` | الجنس |
| `image` | اسم صورة الملف الشخصي |
| `birth_date` | تاريخ الميلاد |
| `is_active` | هل الحساب مفعّل؟ |
| `created_at` | تاريخ الإنشاء |
| `updated_at` | تاريخ آخر تحديث |

الحسابات الجديدة تُنشأ افتراضياً غير مفعّلة إذا كان مخطط القاعدة مضبوطاً على ذلك. يستطيع المشرف تفعيل الحساب من لوحة الإدارة.

### جدول التصنيفات `categories`

يحتوي على اسم التصنيف ووصفه وأيقونته. ترتبط به الدورات من خلال `category_id`.

### جدول المدربين `instructors`

يخزن اسم المدرب وتخصصه ونبذته وبريده. قد ترتبط الدورة بمدرب واحد، ويمكن أن يكون المدرب فارغاً إذا لم يُحدد.

### جدول الدورات `courses`

يخزن عنوان الدورة ووصفها وتصنيفها ومدربها وسعرها ومدتها ومستواها وعدد المقاعد وصورتها وحالتها.

### جدول التسجيلات `enrollments`

يربط المستخدم بالدورة. هذه علاقة many-to-many؛ فالمستخدم يمكن أن يسجل في دورات متعددة، والدورة يمكن أن تحتوي مستخدمين متعددين. يمنع المفتاح الفريد تكرار تسجيل المستخدم في الدورة نفسها.

```text
users 1 ──── * enrollments * ──── 1 courses
categories 1 ──── * courses
instructors 1 ──── * courses
```

حالة التسجيل يمكن أن تكون `pending` أو `active` أو `completed` أو `cancelled`.

### جدول الرسائل `messages`

يخزن رسائل نموذج التواصل مع الاسم والبريد والعنوان والنص وحالة القراءة وتاريخ الإنشاء.

---

## 9. إنشاء الحساب والتحقق من البيانات

في `register.php` لا يحفظ التطبيق البيانات مباشرة. أولاً يقرأ القيم ثم يتحقق منها.

التحققات الحالية تشمل:

- الاسم لا يقل عن ثلاثة أحرف.
- البريد يجب أن يكون بصيغة صحيحة.
- كلمة المرور لا تقل عن ثمانية أحرف.
- تأكيد كلمة المرور يساوي كلمة المرور.
- الجنس محصور في `male` أو `female`.
- البريد لا يتكرر في جدول المستخدمين.

بعد نجاح التحقق تُجزّأ كلمة المرور:

```php
$hash = password_hash($password, PASSWORD_DEFAULT);
```

ثم تُحفظ القيمة المجزأة فقط. لا يحفظ النظام كلمة المرور الأصلية.

بعد إنشاء الحساب يُرسل المستخدم إلى تسجيل الدخول. ويمكن للإدارة تفعيل الحساب حسب سياسة الموقع.

### لماذا لا نخزن كلمة المرور كما هي؟

لأن تسريب قاعدة البيانات سيكشف كلمات المرور إذا كانت مخزنة كنص عادي. التجزئة عملية أحادية الاتجاه؛ التطبيق لا يحتاج إلى معرفة كلمة المرور الأصلية، بل يقارنها بالقيمة المجزأة باستخدام `password_verify`.

---

## 10. تسجيل الدخول

في `login.php` يبحث التطبيق عن المستخدم بالبريد باستخدام استعلام محضر، ثم يطبق الترتيب التالي:

1. إذا لم يوجد المستخدم تظهر رسالة مناسبة.
2. إذا كان الحساب غير مفعّل يُمنع الدخول.
3. إذا فشل `password_verify` تُرفض كلمة المرور.
4. عند النجاح تُحفظ هوية المستخدم ودوره في الجلسة.
5. يُعاد توجيه المشرف إلى لوحة الإدارة أو المستخدم إلى لوحة الحساب.

مثال التحقق:

```php
if (password_verify($password, $user['password'])) {
    $_SESSION['user_id'] = $user['id'];
    $_SESSION['user_role'] = $user['role'];
}
```

الأفضل في النسخة الإنتاجية إضافة `session_regenerate_id(true)` بعد الدخول لتقليل خطر Session Fixation. كما يُستحسن إضافة حد لمحاولات الدخول وتسجيل المحاولات الفاشلة.

---

## 11. الجلسات وتسجيل الخروج

الجلسة هي مساحة بيانات على الخادم مرتبطة بمعرف يُرسل إلى المتصفح في Cookie. في NOVA لا يُرسل الدور وكلمة المرور في كل رابط، بل تُقرأ هوية المستخدم من `$_SESSION`.

الدالة:

```php
function isLoggedIn() {
    return isset($_SESSION['user_id']);
}
```

تحدد هل توجد هوية مستخدم في الجلسة.

تسجيل الخروج يجب أن يلغي بيانات الجلسة حتى لا تبقى هوية المستخدم فعالة:

```php
session_start();
session_unset();
session_destroy();
```

بعد ذلك يُعاد المستخدم إلى الصفحة الرئيسية أو صفحة الدخول.

---

## 12. التحقق من الصلاحيات

توجد دالتان أساسيتان في `includes/auth.php`:

```php
function requireLogin() {
    if (!isLoggedIn()) {
        setFlash('error', 'يجب تسجيل الدخول أولاً');
        redirect(SITE_URL . '/login.php');
    }
}
```

تُستخدم للصفحات التي تحتاج مستخدماً مسجلاً، مثل لوحة الحساب والتسجيل في دورة.

```php
function requireAdmin() {
    if (!isAdmin()) {
        setFlash('error', 'ليس لديك صلاحية');
        redirect(SITE_URL . '/login.php');
    }
}
```

تُستخدم في صفحات مجلد `admin`.

إخفاء زر لوحة الإدارة في `header.php` ليس حماية كافية. الحماية الحقيقية هي التحقق داخل الصفحة نفسها بواسطة `requireAdmin()`.

---

## 13. تصفح الدورات والبحث

تستخدم `courses.php` ثلاثة مصادر للفلترة:

- `category` لتحديد التصنيف.
- `search` للبحث في عنوان الدورة.
- `is_active` لعرض الدورات المنشورة فقط.

يبدأ الاستعلام بشكل عام:

```sql
SELECT c.*, cat.name AS cat_name,
       i.full_name AS instructor_name
FROM courses c
LEFT JOIN categories cat ON cat.id = c.category_id
LEFT JOIN instructors i ON i.id = c.instructor_id
WHERE c.is_active = 1
```

ثم تُضاف شروط البحث مع معاملات محضرة. بعد ذلك تُعرض الدورة في بطاقة تحتوي على التصنيف والعنوان والمدة والمستوى والمدرب والسعر وزر التفاصيل.

صفحة `course.php` تعرض تفاصيل دورة واحدة بعد قراءة `id` وتحويله إلى عدد صحيح، ثم تتحقق من أن الدورة موجودة ونشطة.

---

## 14. التسجيل في دورة

`enroll.php` صفحة تنفيذية محمية بـ`requireLogin()`.

تسلسل العمل:

1. التأكد من أن المستخدم مسجل الدخول.
2. قراءة معرف الدورة وتحويله إلى عدد صحيح.
3. التأكد من وجود دورة نشطة.
4. التأكد من عدم وجود تسجيل سابق للمستخدم نفسه.
5. عدّ التسجيلات ومقارنتها بعدد المقاعد.
6. إدخال التسجيل بحالة `pending`.
7. إعادة المستخدم إلى صفحة دوراته برسالة نجاح.

تُستخدم قاعدة البيانات أيضاً لمنع التكرار، وليس الاعتماد على فحص PHP فقط. لذلك يوجد مفتاح فريد على `(user_id, course_id)`.

---

## 15. لوحة الإدارة وCRUD

CRUD تعني العمليات الأربع الأساسية على البيانات:

| الرمز | المعنى | مثال في NOVA |
|---|---|---|
| Create | إنشاء | إضافة دورة أو تصنيف |
| Read | قراءة | عرض قائمة الدورات |
| Update | تعديل | تعديل سعر دورة |
| Delete | حذف | حذف رسالة أو مدرب |

لوحة الإدارة الرئيسية تعرض إحصاءات مثل عدد المستخدمين والدورات والتسجيلات والرسائل غير المقروءة.

صفحات الإدارة الأساسية هي:

- `admin/users.php`: تفعيل المستخدم وتغيير دوره وحذف المستخدم.
- `admin/courses.php`: إضافة وتعديل وحذف الدورات.
- `admin/categories.php`: إدارة التصنيفات.
- `admin/instructors.php`: إدارة المدربين.
- `admin/enrollments.php`: تغيير حالة التسجيل وحذفه.
- `admin/messages.php`: قراءة الرسائل وحذفها.

كل صفحة إدارة يجب أن تبدأ عادةً باستدعاء `requireAdmin()` قبل تنفيذ أي عملية.

---

## 16. التصميم وتجربة المستخدم

ملف `assets/css/style.css` يحتوي على الهوية البصرية العامة. التصميم يدعم اتجاه اللغة العربية `RTL`، ويستخدم:

- ألواناً كحلية وتركوازية مناسبة لمنصة تعليمية.
- بطاقات ذات حدود وظلال خفيفة.
- أزراراً واضحة للأفعال الأساسية.
- شبكة CSS متجاوبة للدورات والتصنيفات.
- استجابات للشاشات الصغيرة.
- نماذج إدخال موحدة.
- حالات نجاح وخطأ وتنبيه.
- رأساً ثابتاً وتذييلاً منظماً.

في `header.php` توجد روابط الموقع العامة وأزرار الدخول والتسجيل والحساب ولوحة الإدارة. ويُعاد استخدام الرأس والتذييل في الصفحات حتى يكون التصميم موحداً.

تحسين تجربة المستخدم لا يعني الألوان فقط. يشمل أيضاً وضوح الأزرار، ترتيب المعلومات، رسائل الأخطاء، سهولة العودة، وعدم ترك المستخدم في صفحة فارغة دون تفسير.

---

## 17. الأمان الموجود في المشروع

### تجزئة كلمات المرور

استخدام `password_hash` و`password_verify` بدلاً من تخزين النص الأصلي.

### PDO وPrepared Statements

تقليل خطر SQL Injection عبر فصل البيانات عن نص SQL.

### التحقق من البيانات

فحص الاسم والبريد وكلمة المرور والتأكيد والجنس قبل الحفظ.

### حماية الصفحات

استخدام `requireLogin()` و`requireAdmin()` لمنع الوصول غير المصرح.

### خصائص الجلسة

استخدام `HttpOnly` و`Secure` مع HTTPS و`SameSite=Lax`.

### تعطيل أخطاء الإنتاج

عدم إظهار تفاصيل الاستثناءات وقاعدة البيانات للمستخدم النهائي.

### حماية الملفات الحساسة

يمنع `.htaccess` الوصول المباشر إلى ملفات مثل مخطط قاعدة البيانات وملفات الإعداد المحلية، كما يستبعد `.git` والملفات الحساسة من النشر.

---

## 18. نقاط يجب ذكرها بصدق إذا سُئلت عن التطوير المستقبلي

النسخة الحالية مناسبة للعرض والتنفيذ الأساسي، لكن توجد تحسينات إنتاجية موصى بها:

1. إضافة CSRF Token لكل نموذج POST.
2. نقل عمليات الحذف والتفعيل من GET إلى POST فقط.
3. إضافة `session_regenerate_id(true)` بعد الدخول.
4. إضافة Rate Limiting لمحاولات الدخول.
5. منع حذف آخر مشرف في النظام.
6. إضافة سجل Audit Log لعمليات الإدارة.
7. استخدام `htmlspecialchars` أو دالة إخراج موحدة في كل القوالب.
8. استخدام بريد حقيقي لإرسال تفعيل الحساب واسترجاع كلمة المرور.
9. إضافة ترحيلات Database Migrations بدلاً من تعديل القاعدة يدوياً.
10. إضافة اختبارات آلية لعمليات التسجيل والدخول والصلاحيات.

الإجابة المناسبة في المناقشة هي: **أدرك هذه التحسينات، والنسخة الحالية تطبق الأساسيات المطلوبة، أما هذه البنود فهي خطة تقوية للإصدار الإنتاجي التالي.**

---

## 19. النشر على السيرفر

تم تجهيز السيرفر ليعمل على macOS مع:

- Apache.
- PHP 8.5.
- PHP-FPM.
- MySQL.
- نطاق DuckDNS.
- شهادة Let’s Encrypt.
- تحويل HTTP إلى HTTPS.

الرابط النهائي:

```text
https://novaeslz.duckdns.org/
```

سجل DNS للنطاق يشير إلى عنوان السيرفر العام. عندما يفتح المستخدم النطاق، يصل الطلب إلى Apache، ثم يمرر Apache ملفات PHP إلى PHP-FPM، بينما يتصل PHP بـMySQL محلياً.

إعداد HTTPS يتكون من ثلاثة أجزاء:

1. شهادة عامة للنطاق.
2. مفتاح خاص محفوظ على السيرفر ولا يُرفع إلى GitHub.
3. VirtualHost على Apache للمنفذ 443، مع إعادة توجيه المنفذ 80 إلى HTTPS.

يجب تجديد الشهادة قبل انتهاء صلاحيتها. كما يجب تحديث عنوان DuckDNS إذا تغير عنوان IP للسيرفر.

---

## 20. خطوات تشغيل المشروع من الصفر

1. تثبيت Apache وPHP وMySQL.
2. إنشاء قاعدة باسم `nova_db`.
3. إنشاء مستخدم قاعدة بيانات محدود الصلاحيات.
4. استيراد `database/schema.sql`.
5. ضبط `NOVA_DB_HOST` و`NOVA_DB_USER` و`NOVA_DB_PASS` و`NOVA_DB_NAME`.
6. ضبط `NOVA_SITE_URL`.
7. رفع ملفات المشروع إلى DocumentRoot.
8. إعطاء مجلد `uploads` صلاحية الكتابة المناسبة.
9. تشغيل PHP-FPM وApache وMySQL.
10. إنشاء حساب مشرف آمن.
11. اختبار الصفحة الرئيسية والدخول والتسجيل ولوحة الإدارة.
12. تفعيل HTTPS وفحص التحويل من HTTP.

---

## 21. سيناريو عرض عملي أمام المناقش

ابدأ بفتح الصفحة الرئيسية واشرح أنها الواجهة العامة للزائر. افتح الدورات، استخدم البحث والتصنيف، ثم افتح تفاصيل دورة.

بعد ذلك أنشئ حساب مستخدم جديد. اشرح أن البيانات تُفحص أولاً، وأن كلمة المرور تُجزّأ قبل التخزين. سجّل الدخول، ثم افتح لوحة المستخدم وملفك الشخصي.

سجّل في دورة، واشرح أن النظام يتحقق من عدم تكرار التسجيل ومن عدد المقاعد. افتح صفحة دوراتي لإظهار التسجيل.

سجّل الخروج، ثم حاول فتح لوحة الإدارة مباشرة. اشرح أن `requireAdmin()` يمنع الوصول غير المصرح.

سجّل بحساب المدير، افتح لوحة الإدارة، أضف تصنيفاً أو دورة، ثم عد إلى الواجهة العامة لإظهار أثر التغيير.

اختم بعرض قاعدة البيانات وشرح العلاقات بين `users` و`courses` و`enrollments`.

---

## 22. أسئلة متوقعة وإجاباتها

### لماذا اخترت PHP؟

اخترت PHP لأنها مناسبة لتطبيقات الويب التقليدية، وتعمل جيداً مع Apache وMySQL، وتسمح بفهم دورة الطلب من البداية إلى النهاية. كما أنها متاحة على كثير من الاستضافات المجانية.

### لماذا استخدمت PDO؟

لأنه يوفر اتصالاً منظماً بقاعدة البيانات ويدعم prepared statements والاستثناءات، كما أن التطبيق يصبح أسهل في الصيانة.

### ما الفرق بين التشفير والتجزئة؟

التشفير يمكن عكسه بمفتاح، أما التجزئة فهي اتجاه واحد. كلمات المرور لا تحتاج إلى فكها؛ لذلك نستخدم التجزئة.

### لماذا لا تحفظ كلمة المرور مباشرة؟

لأن أي تسريب لقاعدة البيانات سيكشف كلمات المستخدمين. تخزين التجزئة يقلل أثر التسريب.

### كيف تمنع SQL Injection؟

باستخدام `prepare()` و`execute()` وعدم دمج مدخلات المستخدم داخل نص SQL مباشرة.

### كيف تمنع المستخدم من دخول لوحة الإدارة؟

كل صفحة إدارية تستدعي `requireAdmin()`، وهذه الدالة تتحقق من وجود جلسة ومن أن الدور يساوي `admin`.

### هل إخفاء زر الإدارة حماية؟

لا. إخفاء الزر تحسين للواجهة فقط. الحماية الحقيقية تكون في الخادم داخل الصفحات الإدارية.

### ما وظيفة `session_start()`؟

تبدأ أو تستعيد جلسة المستخدم حتى يستطيع التطبيق حفظ معرف المستخدم ودوره بين الطلبات.

### ما وظيفة `HttpOnly`؟

تمنع JavaScript العادية من قراءة Cookie الخاصة بالجلسة، وهذا يقلل خطر سرقتها عبر XSS.

### ما وظيفة `Secure`؟

تجعل المتصفح يرسل Cookie عبر HTTPS فقط.

### ما وظيفة `SameSite`؟

تقلل إرسال Cookie في بعض الطلبات القادمة من مواقع أخرى، مما يساعد على تقليل مخاطر CSRF.

### ما العلاقة بين المستخدم والدورة؟

العلاقة many-to-many، وتم تمثيلها بجدول وسيط هو `enrollments`.

### لماذا يوجد مفتاح فريد في التسجيلات؟

لمنع تسجيل المستخدم نفسه في الدورة نفسها أكثر من مرة حتى لو أرسل الطلب عدة مرات.

### لماذا استخدمت Foreign Keys؟

لضمان سلامة العلاقات ومنع وجود تسجيل لدورة أو مستخدم غير موجود.

### ماذا يحدث إذا امتلأت الدورة؟

يحسب النظام عدد التسجيلات ويقارنه بعدد المقاعد قبل تنفيذ INSERT.

### ما وظيفة `LEFT JOIN` في الدورات؟

تسمح بعرض الدورة حتى لو لم يكن لها مدرب أو تصنيف مرتبط، مع إظهار المعلومات المتوفرة.

### ما الفرق بين GET وPOST؟

GET مناسب للقراءة والبحث، أما POST فمناسب لإرسال بيانات أو تنفيذ تغيير. عمليات الحذف والتفعيل الأفضل أن تكون POST مع CSRF Token.

### كيف اختبرت المشروع؟

اختبرت HTTP status للصفحات، فتح الصفحة الرئيسية والدورات والتفاصيل، التسجيل والدخول، حماية الإدارة، تحميل CSS، اتصال MySQL، وHTTPS والتحويل من HTTP.

### ما الذي يمكن تطويره لاحقاً؟

إضافة CSRF، Rate Limiting، استرجاع كلمة المرور بالبريد، سجل عمليات الإدارة، اختبارات آلية، وترحيلات منظمة لقاعدة البيانات.

---

## 23. إجابة مختصرة جداً في دقيقة واحدة

مشروع NOVA منصة تعليمية عربية مبنية بـPHP وMySQL. يحتوي على واجهة عامة للزائر، حسابات للمستخدمين، ملف شخصي، تسجيل في الدورات، ولوحة إدارة بصلاحيات مختلفة. استخدمت PDO وprepared statements لمنع SQL Injection، واستخدمت `password_hash` و`password_verify` لحماية كلمات المرور، كما استخدمت جلسات PHP مع خصائص HttpOnly وSecure وSameSite. قاعدة البيانات مقسمة إلى مستخدمين وتصنيفات ومدربين ودورات وتسجيلات ورسائل، وجدول التسجيلات يربط المستخدمين بالدورات. نُشر المشروع على Apache وPHP-FPM وMySQL، وربط بنطاق DuckDNS مع شهادة Let’s Encrypt وتحويل HTTPS.

---

## 24. قائمة فحص قبل المناقشة

- [ ] أستطيع شرح فكرة المشروع خلال دقيقة.
- [ ] أستطيع شرح الفرق بين الزائر والمستخدم والمشرف.
- [ ] أعرف وظيفة كل ملف داخل `includes`.
- [ ] أعرف معنى PDO وprepared statements.
- [ ] أعرف الفرق بين `password_hash` و`password_verify`.
- [ ] أعرف كيف تبدأ الجلسة وكيف يحدث تسجيل الخروج.
- [ ] أعرف علاقة `users` و`courses` و`enrollments`.
- [ ] أستطيع شرح عملية التسجيل في دورة خطوة بخطوة.
- [ ] أستطيع شرح CRUD في لوحة الإدارة.
- [ ] أعرف لماذا HTTPS مهم.
- [ ] أعرف أن الدومين المجاني قد يعتمد على استمرار خدمة DNS وثبات IP.
- [ ] أذكر التحسينات المستقبلية بوضوح ولا أدعي وجود وظيفة غير مطبقة.

---

## 25. مراجع تقنية

[1]: https://www.php.net/manual/en/pdo.prepared-statements.php "PHP PDO Prepared Statements"
[2]: https://www.php.net/manual/en/function.password-hash.php "PHP password_hash"
[3]: https://www.php.net/manual/en/function.password-verify.php "PHP password_verify"
[4]: https://www.php.net/manual/en/book.session.php "PHP Sessions"
[5]: https://dev.mysql.com/doc/refman/8.0/en/constraints-foreign-key.html "MySQL Foreign Key Constraints"
[6]: https://httpd.apache.org/docs/2.4/ssl/ssl_howto.html "Apache SSL/TLS Encryption"
[7]: https://letsencrypt.org/docs/ "Let’s Encrypt Documentation"

---

## خاتمة

قوة مشروع NOVA ليست في عدد الصفحات فقط، بل في ربط الواجهة بقاعدة البيانات وتطبيق الصلاحيات ودورة الحساب كاملة. عند شرح المشروع ركّز على سبب كل قرار: لماذا PDO، لماذا التجزئة، لماذا الجلسات، لماذا جدول التسجيلات، ولماذا حماية الإدارة من الخادم. إذا سُئلت عن نقطة غير مطبقة، اذكرها كتحسين مستقبلي بصدق، ثم اشرح كيف ستنفذها.
