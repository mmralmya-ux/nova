# ملف التسليم الأكاديمي — NOVA

**اسم المشروع:** NOVA — منصة تعليمية عربية

**إعداد وتطوير:** المهندس طاهر القاضي

## مطابقة المتطلبات

| المتطلب | التطبيق داخل المشروع |
|---|---|
| واجهات الزوار | الصفحة الرئيسية، عن المعهد، الدورات، تفاصيل الدورة، البحث، التواصل، التسجيل وتسجيل الدخول |
| واجهات المستخدم | لوحة المستخدم، دوراتي، التسجيل في الدورات، الملف الشخصي، تعديل البيانات والصورة، تسجيل الخروج |
| واجهات الإدارة | لوحة الإدارة وإدارة المستخدمين والدورات والتصنيفات والمدربين والتسجيلات والرسائل |
| قاعدة البيانات | `users`, `categories`, `instructors`, `courses`, `enrollments`, `messages` في `database/schema.sql` |
| الحسابات والملفات الشخصية | إنشاء حساب، بريد فريد، بيانات شخصية، صورة، تاريخ ميلاد، وتعديل الملف الشخصي |
| الصلاحيات | أدوار `user` و`admin` مع حماية `requireLogin()` و`requireAdmin()` |
| الجلسات | جلسة PHP بعد تسجيل الدخول مع خصائص `HttpOnly` و`SameSite` و`Secure` عند HTTPS |
| تسجيل الخروج | `logout.php` يدمر الجلسة ويعيد المستخدم إلى الموقع |
| حماية كلمات المرور | `password_hash()` عند التخزين و`password_verify()` عند تسجيل الدخول |
| التحقق من البيانات | التحقق من الاسم والبريد وكلمة المرور والجنس والملفات قبل الحفظ |
| استعلامات قاعدة البيانات | PDO و`prepared statements` لمنع حقن SQL |
| التصميم | RTL عربي، CSS3 متجاوب، بطاقات، بحث، حالات فارغة، وتدرج لوني موحد |
| الوسائط | صور للدورات والملفات الشخصية وروابط فيديو، مع تحديد الأنواع والأحجام |
| النشر | Apache وPHP-FPM وMySQL وHTTPS في بيئة الإنتاج |

## نقاط العرض أمام المناقش

يبدأ العرض بشرح الفرق بين الزائر والمستخدم والمدير. ثم يُعرض إنشاء الحساب، تسجيل الدخول، الصفحة الشخصية، التسجيل في دورة، ثم لوحة الإدارة وتفعيل الحساب وإدارة المحتوى. بعد ذلك يُشرح مخطط الجداول والعلاقات، ثم سبب استخدام PDO و`prepared statements` و`password_hash`، وأخيراً إعدادات HTTPS وخصائص جلسة PHP.

## ملاحظات التشغيل

يجب ضبط متغيرات البيئة الخاصة بقاعدة البيانات والرابط قبل التشغيل. لا تُحفظ كلمات المرور أو مفاتيح الخدمات داخل المستودع. يجب أن تكون مجلدات `uploads/courses` و`uploads/users` قابلة للكتابة من خادم الويب، مع منع تنفيذ ملفات PHP داخلها عبر إعدادات الخادم.

هذا الملف مخصص لتوثيق نطاق النسخة الأكاديمية، بينما يشرح `NOVA-discussion-guide-ar.md` تفاصيل المناقشة والملفات ودورة تنفيذ الطلب.

— المهندس طاهر القاضي

